<h1>Pages</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Email</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @pages.each do |page| %>
    <tr>
      <td><%= page.title %></td>
      <td><%= link_to "Edit", edit_admin_page_path(page) %></td>
    </tr>
    <% end %>
  </tbody>
</table>